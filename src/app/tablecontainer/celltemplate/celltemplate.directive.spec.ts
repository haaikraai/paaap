import { CelltemplateDirective } from './celltemplate.directive';

describe('CelltemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new CelltemplateDirective();
    expect(directive).toBeTruthy();
  });
});
